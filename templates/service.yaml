apiVersion: v1
kind: Service
metadata:
  name: {{ template "workbench.fullname" . }}
  labels:
    app: {{ template "workbench.name" . }}
    chart: {{ template "workbench.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "workbench.name" . }}
    release: {{ .Release.Name }}
