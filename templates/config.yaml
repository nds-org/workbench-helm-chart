apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}
  namespace: {{ .Release.Namespace }}
data:
  frontend.json: |
    {{ .Values.frontend | toJson }}
  backend.json: |
    {{ .Values.backend | toJson }}

